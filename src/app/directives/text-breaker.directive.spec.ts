import { TextBreakerDirective } from './text-breaker.directive';

describe('TextBreakerDirective', () => {
  it('should create an instance', () => {
    const directive = new TextBreakerDirective();
    expect(directive).toBeTruthy();
  });
});
